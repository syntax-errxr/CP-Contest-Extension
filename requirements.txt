# Note: Since the backend has been migrated to TypeScript/Node.js, 
# dependencies are managed via NPM (package.json) instead of Python's pip.
# You do NOT need to run pip install.

# How to install dependencies:
# 1. In /backend, run: npm install
# 2. In /frontend, run: npm install

# Key Technology Stack Dependencies:
# - Node.js (version 18+)
# - Express (web framework)
# - Prisma Client (database client)
# - SQLite (database file test.db)
# - React + TypeScript (frontend framework)
# - Vite (frontend bundler)
